// Number of operations to Make network connected;

// here we have given set of connected components of network and number of wires that ins number of edges and we have to tell weather we can 
//  make network connected or not if we can then return minimum number of operations


//  This Problem is semilar like number of islands problem but here ans is number of components - 1.

// also one extra case if check weather the number of  computer - 1 are greater than connections.size();
// ie. if(connections.size()<n-1) return -1